<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Circle ImageView Style -->
    <style name="CircleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Rounded ImageView Style -->
    <style name="RoundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <style name="CircleBackground" parent="">
        <item name="android:background">@drawable/circle_background</item>
    </style>

    <style name="Theme.SethPOS.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.SethPOS.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources> 